doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h4 Welcome to sports manger
        form(action = "/uploadsports", method = "POST" enctype="multipart/form-data")

            div
                label(for = "upload") Uplaoadfile
                input(type="file" name = "upload" id='upload' multiple)
                button(type = "submit") Submit 
